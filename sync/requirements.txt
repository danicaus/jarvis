# psycopg2 é instalado via apt (sudo apt install python3-psycopg2), não por
# aqui — evita o bloqueio "externally-managed-environment" do Python do
# sistema (PEP 668). Deixado comentado só como registro da dependência.
# psycopg2-binary

# Transcrição de áudio — faster-whisper, num venv separado (sync/.venv),
# não no Python de sistema. Setup (uma vez só, sem sudo):
#   python3 -m venv sync/.venv
#   sync/.venv/bin/pip install faster-whisper
#
# Trocado do `openai-whisper` (via pipx) em 16/set/2026: mesmo modelo
# "small", mas ~5x menos RAM de pico (770MB vs. o servidor de 3,8GB ficar
# sem memória) e bem mais rápido, rodando via CTranslate2/int8 em vez do
# pacote de referência da OpenAI. "medium" foi testado e descartado — ~2GB
# de pico, sem ganho de qualidade consistente.
